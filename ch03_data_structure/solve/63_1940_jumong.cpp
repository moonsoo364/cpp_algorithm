#include <iostream>
#include <vector>

using namespace std;

int main(){
    cin.tie(0);
    cout.tie(0);

    int n, m;
    cin >> n >> m;
    vector<int> v;
    v.assign(n,0);
    
    for(int i = 0; i < i ++){
        cin >> v[i];
    }

    

    return 0;
}