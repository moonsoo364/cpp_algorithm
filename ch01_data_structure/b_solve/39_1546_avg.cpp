#include <iostream>
#include <vector>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n, sum, avg;
    vector<int> v;
    cin >> n;
    v.assign(0,n);
    
    for(int i : v){
        sum += i;
    }
    avg = sum
    return 0;
}